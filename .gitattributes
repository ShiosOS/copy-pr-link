# Normalize line endings to LF on checkout/commit for all text files so the
# repo is consistent across Windows, macOS, and Linux CI. Prettier and
# .editorconfig also enforce LF.
* text=auto eol=lf

# Binary assets must never be normalized.
*.png binary
*.xpi binary
*.zip binary
